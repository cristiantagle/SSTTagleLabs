<UserControl x:Class="TagleLabsGestorSST.UI.Views.PreviewFluentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="FluentBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F4F6FB" Offset="0"/>
            <GradientStop Color="#EEF2F9" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="FluentAccent" Color="#2563EB"/>
        <SolidColorBrush x:Key="FluentText" Color="#0F172A"/>
        <SolidColorBrush x:Key="FluentSubText" Color="#64748B"/>
        <Style x:Key="FluentCard" TargetType="Border">
            <Setter Property="Background" Value="#F7FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0F172A" BlurRadius="20" ShadowDepth="4" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FluentButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource FluentAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="FluentChip" TargetType="Border">
            <Setter Property="Background" Value="#EEF2FF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource FluentBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left navigation -->
        <Border Grid.Column="0" Background="#0B1220">
            <StackPanel Margin="16,24,16,16">
                <TextBlock Text="TagleLabs" Foreground="White" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="Gestor SST" Foreground="#94A3B8" FontSize="12" Margin="0,2,0,18"/>
                <Border Background="#1E293B" CornerRadius="8" Padding="10" Margin="0,0,0,8">
                    <TextBlock Text="Dashboard" Foreground="White" FontWeight="SemiBold"/>
                </Border>
                <TextBlock Text="Documentos" Foreground="#94A3B8" Margin="8,6"/>
                <TextBlock Text="Trabajadores" Foreground="#94A3B8" Margin="8,6"/>
                <TextBlock Text="Obligaciones" Foreground="#94A3B8" Margin="8,6"/>
            </StackPanel>
        </Border>

        <!-- Main -->
        <Grid Grid.Column="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Panel de Control" Foreground="{StaticResource FluentText}" FontSize="26" FontWeight="Bold"/>
                    <TextBlock Text="Vista Fluent con cards y acciones suaves" Foreground="{StaticResource FluentSubText}" FontSize="12"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource FluentButton}" Content="Nuevo reporte"/>
            </Grid>

            <UniformGrid Grid.Row="1" Columns="3" Margin="0,18,0,18">
                <Border Style="{StaticResource FluentCard}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Cumplimiento" Foreground="{StaticResource FluentSubText}" FontSize="12"/>
                        <TextBlock Text="87%" Foreground="{StaticResource FluentText}" FontSize="24" FontWeight="Bold" Margin="0,6,0,0"/>
                        <ProgressBar Height="6" Value="87" Margin="0,10,0,0" Background="#E2E8F0" Foreground="#22C55E"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource FluentCard}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Pendientes" Foreground="{StaticResource FluentSubText}" FontSize="12"/>
                        <TextBlock Text="14" Foreground="{StaticResource FluentText}" FontSize="24" FontWeight="Bold" Margin="0,6,0,0"/>
                        <TextBlock Text="3 vencen esta semana" Foreground="{StaticResource FluentSubText}" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource FluentCard}">
                    <StackPanel>
                        <TextBlock Text="Alertas" Foreground="{StaticResource FluentSubText}" FontSize="12"/>
                        <TextBlock Text="2" Foreground="{StaticResource FluentText}" FontSize="24" FontWeight="Bold" Margin="0,6,0,0"/>
                        <Border Style="{StaticResource FluentChip}" Margin="0,8,0,0">
                            <TextBlock Text="Evaluacion critica" Foreground="#4338CA" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource FluentCard}" Grid.Column="0" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Actividad reciente" Foreground="{StaticResource FluentText}" FontWeight="SemiBold"/>
                        <Separator Margin="0,8,0,8"/>
                        <TextBlock Text="Documento RIOHS actualizado" Foreground="{StaticResource FluentText}" Margin="0,4"/>
                        <TextBlock Text="Siniestro registrado (Planta)" Foreground="{StaticResource FluentText}" Margin="0,4"/>
                        <TextBlock Text="Vacaciones aprobadas" Foreground="{StaticResource FluentText}" Margin="0,4"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource FluentCard}" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="Acciones rapidas" Foreground="{StaticResource FluentText}" FontWeight="SemiBold"/>
                        <Separator Margin="0,8,0,8"/>
                        <Button Style="{StaticResource FluentButton}" Content="Importar documentos" Margin="0,0,0,6"/>
                        <Button Background="#E2E8F0" Foreground="#1F2937" Padding="12,8" Content="Generar reporte"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
