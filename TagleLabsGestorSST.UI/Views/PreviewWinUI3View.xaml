<UserControl x:Class="TagleLabsGestorSST.UI.Views.PreviewWinUI3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="MicaLike" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F7F7FA" Offset="0"/>
            <GradientStop Color="#EEF0F6" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="WinAccent" Color="#0078D4"/>
        <SolidColorBrush x:Key="WinText" Color="#1B1B1F"/>
        <SolidColorBrush x:Key="WinSubText" Color="#5A5F6A"/>
        <Style x:Key="WinCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderBrush" Value="#E6E8EF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0B1220" BlurRadius="18" ShadowDepth="3" Opacity="0.06"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WinButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource WinAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="WinTag" TargetType="Border">
            <Setter Property="Background" Value="#F1F5FF"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="6,2"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource MicaLike}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top command bar -->
        <Border Background="#FFFFFF" BorderBrush="#E6E8EF" BorderThickness="0,0,0,1" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="TagleLabs SST" Foreground="{StaticResource WinText}" FontSize="16" FontWeight="SemiBold"/>
                <TextBox Grid.Column="1" Margin="16,0" Height="30" VerticalContentAlignment="Center"
                         Text="Buscar documentos, trabajadores..." Foreground="{StaticResource WinSubText}"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource WinButton}" Content="Nuevo"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Side rail -->
            <Border Background="#FFFFFF" BorderBrush="#E6E8EF" BorderThickness="1" CornerRadius="10" Padding="12">
                <StackPanel>
                    <TextBlock Text="Navegacion" Foreground="{StaticResource WinSubText}" FontSize="11" Margin="0,0,0,8"/>
                    <TextBlock Text="Dashboard" Foreground="{StaticResource WinText}" FontWeight="SemiBold" Margin="6,4"/>
                    <TextBlock Text="Documentos" Foreground="{StaticResource WinSubText}" Margin="6,4"/>
                    <TextBlock Text="Trabajadores" Foreground="{StaticResource WinSubText}" Margin="6,4"/>
                    <TextBlock Text="Siniestros" Foreground="{StaticResource WinSubText}" Margin="6,4"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                <TextBlock Text="Resumen" Foreground="{StaticResource WinText}" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock Text="Diseño WinUI 3: limpio, espacioso y claro" Foreground="{StaticResource WinSubText}" FontSize="12" Margin="0,2,0,12"/>

                <UniformGrid Columns="2">
                    <Border Style="{StaticResource WinCard}" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Inspecciones" Foreground="{StaticResource WinSubText}" FontSize="12"/>
                            <TextBlock Text="32" Foreground="{StaticResource WinText}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,0"/>
                            <Border Style="{StaticResource WinTag}" Margin="0,10,0,0">
                                <TextBlock Text="Actualizado hoy" Foreground="#2D4F9E" FontSize="11"/>
                            </Border>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource WinCard}" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="No conformidades" Foreground="{StaticResource WinSubText}" FontSize="12"/>
                            <TextBlock Text="5" Foreground="{StaticResource WinText}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,0"/>
                            <TextBlock Text="2 criticas" Foreground="#B91C1C" FontSize="12" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Border Style="{StaticResource WinCard}">
                    <StackPanel>
                        <TextBlock Text="Lista corta" Foreground="{StaticResource WinText}" FontWeight="SemiBold"/>
                        <Separator Margin="0,8,0,8"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Plan anual SST 2025" Foreground="{StaticResource WinText}"/>
                            <TextBlock Grid.Column="1" Text="OK" Foreground="#16A34A"/>
                        </Grid>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Programa prevencion" Foreground="{StaticResource WinText}"/>
                            <TextBlock Grid.Column="1" Text="Pendiente" Foreground="#D97706"/>
                        </Grid>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Auditoria interna" Foreground="{StaticResource WinText}"/>
                            <TextBlock Grid.Column="1" Text="En curso" Foreground="#2563EB"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
