<UserControl x:Class="TagleLabsGestorSST.UI.Views.AuditoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="#F1F5F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER GRADIENTE -->
        <Border Style="{StaticResource EstiloHeaderHero}" Grid.RowSpan="2"/>

        <!-- CONTENIDO -->
        <Grid Grid.Row="1" Margin="30,-80,30,30">
            <StackPanel>
                <Grid Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Auditoría del Sistema" Style="{StaticResource EstiloTituloPagina}" Margin="0,0,0,5"/>
                        <TextBlock Text="Registro de actividad y cambios" Style="{StaticResource EstiloSubtituloPagina}"/>
                    </StackPanel>
                    <Button Command="{Binding CargarLogsCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" 
                            HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="#10B981" BorderBrush="#10B981" ToolTip="Actualizar">
                        <materialDesign:PackIcon Kind="Refresh" Foreground="White"/>
                    </Button>
                </Grid>

                <Border Style="{StaticResource EstiloCardContenido}" Padding="0">
                    <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="Transparent" BorderThickness="0" MaxHeight="650"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="Background" Value="White"/>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="FECHA" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy HH:mm'}" FontWeight="Bold" Foreground="#0F172A"/>
                            <DataGridTextColumn Header="USUARIO" Binding="{Binding Usuario}" Foreground="#64748B"/>
                            <DataGridTemplateColumn Header="ACCIÓN">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#F1F5F9" CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Accion}" FontSize="11" FontWeight="SemiBold" Foreground="#475569"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="ENTIDAD" Binding="{Binding Entidad}" Foreground="#64748B"/>
                            <DataGridTextColumn Header="DETALLE" Binding="{Binding Detalle}" Width="*" Foreground="#0F172A"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
